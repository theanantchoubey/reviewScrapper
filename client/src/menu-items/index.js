import about from './about';
import other from './other';
import services from './services';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [other, services, about]
};

export default menuItems;
